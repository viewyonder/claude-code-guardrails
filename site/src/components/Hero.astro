---
---

<section class="hero" id="hero">
  <div class="container hero-inner">
    <h1 class="glitch" data-text="Coherence for Claude">Coherence for Claude</h1>
    <p class="hero-subtitle">
      <span class="typed" id="typed-subtitle"></span><span class="cursor"></span>
    </p>
    <p class="hero-desc">
      A system of hooks, agents, skills, and specification documents that encode your project's
      architectural constraints into the Claude Code development loop.
    </p>
    <div class="hero-actions">
      <a href="/getting-started" class="btn btn-primary">Get Started</a>
      <a
        href="https://github.com/viewyonder/coherence"
        class="btn btn-secondary"
        target="_blank"
        rel="noopener noreferrer"
      >
        View on GitHub
      </a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding-top: 4rem;
    position: relative;
  }

  .hero-inner {
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .hero h1 {
    margin-bottom: 1.5rem;
  }

  .hero-subtitle {
    font-family: var(--font-mono);
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: var(--cyan);
    margin-bottom: 2rem;
    min-height: 1.8em;
  }

  .hero-desc {
    font-size: 1.15rem;
    color: var(--text-secondary);
    max-width: 60ch;
    margin: 0 auto 2.5rem;
    line-height: 1.8;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

</style>
