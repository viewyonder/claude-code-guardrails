---
---

<section id="blog-cta" class="scanlines">
  <div class="container">
    <div class="reveal cta-inner">
      <div class="pull-quote">
        The quality of generated code is bounded not by model capability but by the quality
        of the architectural knowledge available at generation time.
      </div>
      <p class="cta-text">
        Read the full rationale behind this approach — why obvious solutions fail,
        how the four layers interact, and what this changes about the role of senior engineers.
      </p>
      <a href="/blog/entropy-at-velocity" class="btn btn-primary">
        Read "Entropy at Velocity"
      </a>
    </div>
  </div>
</section>

<style>
  #blog-cta {
    background: var(--bg-mid);
    border-top: 1px solid var(--bg-surface);
    border-bottom: 1px solid var(--bg-surface);
  }

  .cta-inner {
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }

  .cta-inner .pull-quote {
    text-align: left;
    margin-bottom: 2rem;
  }

  .cta-text {
    color: var(--text-secondary);
    margin-bottom: 2rem;
    font-size: 1.05rem;
  }

</style>
