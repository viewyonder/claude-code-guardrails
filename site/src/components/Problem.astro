---
---

<section class="problem scanlines" id="problem">
  <div class="container">
    <div class="reveal">
      <p class="section-label">The Problem</p>
      <h2 class="section-title">Entropy at Velocity</h2>
      <div class="pull-quote">
        AI-assisted development creates entropy faster than traditional development, and that entropy
        degrades the AI's effectiveness.
      </div>
      <div class="problem-grid grid-2">
        <div class="problem-card">
          <h3>You drift from the architecture</h3>
          <p>
            The agent doesn't know your unwritten conventions. It generates perfectly functional code
            that violates boundaries it's never been told about. Not negligence — ignorance.
          </p>
        </div>
        <div class="problem-card">
          <h3>The architecture drifts from reality</h3>
          <p>
            Your spec says 14 inspectors. You have 18. Your API reference lists 23 endpoints.
            The codebase has 31. Stale specs are worse than no specs.
          </p>
        </div>
      </div>
      <p class="problem-cta">
        <a href="/blog/entropy-at-velocity">Read the full analysis &rarr;</a>
      </p>
    </div>
  </div>
</section>

<style>
  .problem {
    background: var(--bg-mid);
    border-top: 1px solid var(--bg-surface);
    border-bottom: 1px solid var(--bg-surface);
  }

  .problem-grid {
    margin-top: 2rem;
  }

  .problem-card {
    background: var(--bg-surface);
    border-radius: var(--radius);
    padding: 1.5rem;
    border: 1px solid rgba(255, 255, 255, 0.05);
  }

  .problem-card h3 {
    color: var(--magenta);
    margin-bottom: 0.75rem;
    font-size: 1.1rem;
  }

  .problem-card p {
    color: var(--text-secondary);
    font-size: 0.95rem;
  }

  .problem-cta {
    margin-top: 2rem;
    text-align: center;
    font-family: var(--font-mono);
    font-size: 0.9rem;
  }
</style>
